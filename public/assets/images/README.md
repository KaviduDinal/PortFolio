Place the following image files here (used by EducationLottie):

- me.png
- next.svg
- globe.svg
- file.svg
- window.svg
- vercel.svg

Files should be named exactly as above. After adding them, the component will load them via `/assets/images/<name>`.

If you prefer imports instead of public assets, move images into `src/assets/images` and update the component accordingly.